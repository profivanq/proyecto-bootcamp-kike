# Conexión a la base de datos Neon Postgres.
# En Vercel, al conectar Neon desde Storage, esta variable se crea automáticamente.
# Para desarrollo local, copia este archivo a .env.local y pega tu cadena de conexión.
DATABASE_URL="postgresql://usuario:password@host.neon.tech/dbname?sslmode=require"
